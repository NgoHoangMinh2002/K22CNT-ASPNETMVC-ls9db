@model IEnumerable<NhmLesson10db.Models.Khoa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "NhmCreate")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TenKH)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenKH)
        </td>
        <td>
            @Html.ActionLink("Edit", "NhmEdit", new { id=item.MaKH }) |
            @Html.ActionLink("Details", "NhmDetails", new { id=item.MaKH }) |
            @Html.ActionLink("Delete", "NhmDelete", new { id=item.MaKH })
        </td>
    </tr>
}

</table>
